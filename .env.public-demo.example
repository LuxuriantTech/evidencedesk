# Copier vers .env.public-demo (ignoré par Git) et remplacer les deux valeurs.
# Ne jamais exposer la pile avec ces placeholders.
PUBLIC_DEMO_JWT_SECRET=replace-with-at-least-32-random-characters
PUBLIC_DEMO_ANALYST_PASSWORD=replace-with-a-unique-demo-password
